# For local development (Vite proxies /api to localhost:8000 automatically)
# Only needed when deploying to production

# Replace with your deployed backend URL
VITE_API_URL=http://localhost:8000/api/v1
